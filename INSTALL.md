From the repository root or package directory:
```bash
git clone https://github.com/jabulu/led-ml.git
cd led-ml/led-ml
python3 -m pip install -e .
```

From PyPI install
```bash
python3 -m pip install led-ml
```
